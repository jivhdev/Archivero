<Window x:Class="Archivero.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Archivero" Height="480" Width="640"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="16" FontWeight="Bold" Text="Archivero" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Name="TxtCarpetaObservada" Foreground="Gray" Margin="0,0,0,16" TextWrapping="Wrap"/>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Pendientes por reconocer" FontWeight="Bold" Margin="0,0,0,8"/>
            <ListBox Grid.Row="1" Name="ListaPendientes" DisplayMemberPath="NombreArchivo" MouseDoubleClick="ListaPendientes_MouseDoubleClick"/>
        </Grid>
    </Grid>
</Window>
